# powblock

Ephemeral proof-of-work public storage

## Create

Server stores `content` at `uuid` for `hours` if pow is satisfied. Revision is always `1` on creation. Server limits source IP addresses to one block creation per minute.

To prevent reuse, uuid is a timestamp-checked [ULID](https://github.com/ulid/spec). ULIDs are user-selected rather that server-generated so that they can be included in the POW and avoid replays while allowing a single POST to create a block.

On creation, a hash of `secret` is stored with the block and used to authorize future operations. Secret is generated by the client and may be any printable ASCII string up to 32 bytes in length.

## Create or Update

```
PUT /powblocks/01J8Q2V6X7Y8Z9A0B1C2D3E4F5
Content-Type: application/json
Authorization: Bearer secret

{
  "content": "This is the data stored in the block.",
  "hours": 24,
  "modified": "01J8Q2V6X7",
  "pow": "000000ef57...",
  "nonce": 1844674407,
}

HTTP/1.1 201 Created
Content-Type: application/json
{
  "expires": 1787925546,
}
```

`modified` must be greater than stored `modified` or update will fail.

## Read

```
GET /powblocks/01J8Q2V6X7Y8Z9A0B1C2D3E4F5


HTTP/1.1 200 OK
{
  "content": "This is the data stored in the block.",
}
```

## Vacuuming

- Blocks are removed after they reach their expiry time.

## Proof of Work

pow = H(uuid + modfied + content + nonce) < 2^256 / (work_factor * max(bytes,256) * max(hours, 1))

## Data Model

Data is stored in single sqlite table with the following scheme:

```sql
create table powblocks (
  uuid text primary key
    collate binary
    check (length(uuid) = 26),

  revision integer not null default 1
    check (revision >= 1),

  content blob not null
    check (length(content) <= 65536),

  created_at integer not null,
  updated_at integer not null,
  expires_at integer not null,

  src_ip text not null,

  secret_hash blob not null
    check (length(secret_hash) = 32)
);
```
